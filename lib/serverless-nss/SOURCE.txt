Shared libraries copied from amazonlinux:2023 (x86_64) RPMs: nspr, nss, nss-util, nss-softokn, nss-softokn-freebl.
Used at runtime on minimal serverless images (e.g. Vercel) that ship no NSPR/NSS.
Regenerate: docker run --platform linux/amd64 --rm -v "$PWD/lib/serverless-nss:/out" amazonlinux:2023 bash -c 'dnf install -y nss nss-util nspr nss-softokn nss-softokn-freebl && cd /usr/lib64 && for b in libnspr4 libplc4 libplds4 libsmime3 libssl3; do for f in ${b}.so*; do [ -e "$f" ] && cp -a "$f" /out/; done; done'
